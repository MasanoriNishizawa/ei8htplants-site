# BD-02 — 画面遷移図・画面UI設計 (Screen Transition & UI Design)

## 1. 画面遷移図

```
[ブラウザURL入力 / リンク]
        │
        ▼
【ブランドサイト】
┌───────────────────────────────────────────────────────────────┐
│ Layout（Header + Footer 共通）                                 │
│  Header: ロゴ + ブランドナビ + [SHOP] CTAボタン                 │
│                                                               │
│  /              → Home                                        │
│  /events        → Events ──── [予約ボタン] ──► /reserve        │
│  /events/:id    → EventSite（イベント専用ページ）              │
│  /gallery       → Gallery                                     │
│  /concept       → Concept                                     │
│  /contact       → Contact                                     │
│  /stockists     → Stockists                                   │
│  /collaborations→ Collaborations                              │
│  /ei8htplants         → Ei8htPlants                           │
│  /habitatoides        → HabitatOides                          │
│  /habitatoides/workshop → HabitatOidesWorkshop               │
│  /hue                 → HUE by ei8ht plants                   │
│  /legal               → LegalPage                             │
└───────────────────────────────────────────────────────────────┘

【ストア（EC）】
┌───────────────────────────────────────────────────────────────┐
│ StoreLayout（StoreHeader + Footer 共通）                       │
│  StoreHeader: ロゴ + [Shop][Journal] ナビ + [Official Site]    │
│              + [Cart N] バッジ                                 │
│                                                               │
│  /shop          → Shop（記事4カラム + 商品3カラム + カテゴリーフィルター）│
│  /shop/:id      → ShopProduct ──── [カートに入れる] ──► /checkout│
│  /journal       → Journal（記事一覧・タグフィルター）           │
│  /journal/:id   → JournalArticle ──── [関連商品] ──► /shop/:id │
│  /checkout      → Checkout（Square決済） ──► /order/complete   │
│  /order/complete→ OrderComplete                               │
│  /reserve       → Reserve（WS予約フォーム）                    │
│  /cancel        → CancelReservation                           │
└───────────────────────────────────────────────────────────────┘

/admin/* → AdminLayout（認証ガード）
    │
    ├── 未認証 → ログインフォーム（モーダル全画面）
    │
    └── 認証済み → サイドバーナビ + 各Admin画面
                    │
                    ├── /admin                        → Dashboard
                    ├── /admin/events                 → Events管理
                    │       ├── /admin/events/new     → EventForm（新規）
                    │       ├── /admin/events/:id/edit→ EventForm（編集）
                    │       ├── /admin/events/:id/finances → EventFinance
                    │       ├── /admin/events/:id/reservations → EventReservations
                    │       └── /admin/events/:id/site→ AdminEventSite
                    ├── /admin/gallery                → Gallery管理
                    ├── /admin/stockists              → Stockists管理
                    ├── /admin/reservations           → Reservations管理
                    ├── /admin/collaborations         → Collaborations管理
                    ├── /admin/contacts               → Contacts管理
                    ├── /admin/products               → Products管理
                    ├── /admin/orders                 → Orders管理
                    └── /admin/articles               → Articles管理
```

---

## 2. ユーザージャーニー（主要フロー）

### イベント確認 → ワークショップ予約

```
Home
  └─ "次のイベント" カード表示
        └─ [予約] ボタン / /events への導線
              ↓
Events (/events)
  └─ イベントカード確認（場所・日時・画像）
        └─ [予約する] ボタン
              ↓
Reserve (/reserve?event_id=<uuid>)
  └─ 予約フォーム入力（名前・メール・参加人数）
        └─ 送信 → 完了メッセージ
```

### ブランドページ → コラボレーション確認

```
HabitatOides (/habitatoides)
  └─ Collaboration セクション
        └─ "View all collaborations →" リンク
              ↓
Collaborations (/collaborations)
  └─ コラボ記事一覧（画像/動画・説明）
```

### お問い合わせ

```
任意のページ
  └─ Header の "Contact" リンク
        ↓
Contact (/contact)
  └─ フォーム入力 → 送信 → 完了メッセージ表示
```

---

## 3. ユーザージャーニー（EC フロー）

```
Shop (/shop)
  ├─ [記事カード] ──► JournalArticle ──► [関連商品]
  └─ [商品カード] ──► ShopProduct
                          └─ [カートに入れる]
                                ↓
                          Checkout (/checkout)
                            └─ Square 決済
                                  ↓
                          OrderComplete (/order/complete)
```

---

## 4. レイアウト構造

### ブランドサイト（Layout.tsx）

```
┌────────────────────────────────────────┐
│ Header（固定, height: 60px）           │
│  [logo] [nav] [SHOP ボタン] [☰]       │
├────────────────────────────────────────┤
│  main.site-main  padding-top: 60px     │
│  <Outlet />                            │
├────────────────────────────────────────┤
│ Footer                                 │
└────────────────────────────────────────┘
```

### ストア（StoreLayout.tsx）

```
┌────────────────────────────────────────┐
│ StoreHeader（固定, height: 56px）      │
│  [logo] [Shop] [Journal]  [Official Site] [Cart N]│
├────────────────────────────────────────┤
│  main  padding-top: 56px               │
│  <Outlet />                            │
├────────────────────────────────────────┤
│ Footer                                 │
└────────────────────────────────────────┘
```

### 管理画面（AdminLayout.tsx）

```
PC: aside(200px) + main(flex:1)
モバイル: ハンバーガー → aside が fixed overlay ドロワーで展開

サイドバー項目: ダッシュボード / イベント / ギャラリー / 取扱店
              / WS予約 / コラボレーション / お問い合わせ
              / 商品 / 注文 / 記事
```

### ブランドページ（サブナビ付き）

```
┌────────────────────────────────────────┐
│ Header（60px）                         │
├────────────────────────────────────────┤
│ .hero-wrapper                          │
│  ├── .brand-subnav（固定サブナビ）      │
│  │    [Concept] [Workshop] [...]       │
│  └── .hero-section（ヒーロービジュアル）│
├────────────────────────────────────────┤
│ section#concept                        │
├────────────────────────────────────────┤
│ section#workshop                       │
├────────────────────────────────────────┤
│ section#collaboration                  │
└────────────────────────────────────────┘
```

---

## 5. ルーティング定義（App.tsx）

### ブランドサイト（Layout）

| パス | コンポーネント | 備考 |
|---|---|---|
| `/` | `Home` | |
| `/events` | `Events` | |
| `/events/:id` | `EventSite` | イベント専用公開ページ |
| `/gallery` | `Gallery` | |
| `/concept` | `Concept` | |
| `/contact` | `Contact` | |
| `/stockists` | `Stockists` | |
| `/collaborations` | `Collaborations` | |
| `/ei8htplants` | `Ei8htPlants` | |
| `/habitatoides` | `HabitatOides` | |
| `/habitatoides/workshop` | `HabitatOidesWorkshop` | |
| `/hue` | `Hue` | |
| `/legal` | `LegalPage` | 特定商取引法・プライバシーポリシー |

### ストア（StoreLayout）

| パス | コンポーネント | 備考 |
|---|---|---|
| `/shop` | `Shop` | 記事カード + 商品グリッド + カテゴリーフィルター |
| `/shop/:id` | `ShopProduct` | |
| `/journal` | `Journal` | |
| `/journal/:id` | `JournalArticle` | |
| `/checkout` | `Checkout` | Square Web Payments SDK |
| `/order/complete` | `OrderComplete` | navigate state で注文情報受け取り |
| `/reserve` | `Reserve` | `?event_id=` URLパラメータ |
| `/cancel` | `CancelReservation` | `?token=` URLパラメータ |

### 管理（AdminLayout, 要認証）

| パス | コンポーネント |
|---|---|
| `/admin` | `Dashboard` |
| `/admin/events` | `admin/Events` |
| `/admin/events/new` | `admin/EventForm` |
| `/admin/events/:id/edit` | `admin/EventForm` |
| `/admin/events/:id/finances` | `admin/EventFinance` |
| `/admin/events/:id/reservations` | `admin/EventReservations` |
| `/admin/events/:id/site` | `admin/AdminEventSite` |
| `/admin/gallery` | `admin/Gallery` |
| `/admin/stockists` | `admin/Stockists` |
| `/admin/reservations` | `admin/Reservations` |
| `/admin/collaborations` | `admin/Collaborations` |
| `/admin/contacts` | `admin/Contacts` |
| `/admin/products` | `admin/Products` |
| `/admin/orders` | `admin/Orders` |
| `/admin/articles` | `admin/Articles` |

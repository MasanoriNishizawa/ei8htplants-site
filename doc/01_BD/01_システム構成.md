# BD-01 — システム構成 (System Architecture)

## 1. アーキテクチャ概要

```
ブラウザ (React SPA)
    │
    │  /api/* → HTTP
    │
    ├── [開発時] Vite dev server (port 5173)
    │               │ proxy /api → localhost:8000
    │               ▼
    └── [本番時] ─── FastAPI (uvicorn, port 8000)
                         │
                         ├── Supabase (PostgreSQL + Auth + RLS)
                         │       ← データ読み書き・管理者認証
                         │
                         └── Resend API
                                 ← メール送信（お問い合わせ受信時）
```

**開発時**: Vite dev server + uvicorn の2プロセス構成。Vite の proxy 設定で `/api` をバックエンドに転送。  
**本番時**: `npm run build` でビルドしたSPAを FastAPI の `StaticFiles` で配信。単一プロセス。

---

## 2. 構成要素

### フロントエンド

| 項目 | 採用技術 | バージョン |
|---|---|---|
| フレームワーク | React | 19.1.0 |
| 言語 | TypeScript | 5.8.3 |
| ビルドツール | Vite | 6.3.5 |
| ルーティング | react-router-dom | 7.6.3 |
| スタイリング | Tailwind CSS + `index.css`（グローバル） | v3 |
| Admin認証クライアント | @supabase/supabase-js | 2.49.8 |

### バックエンド

| 項目 | 採用技術 | バージョン |
|---|---|---|
| フレームワーク | FastAPI | 0.115.5 |
| ASGI サーバー | uvicorn | 0.32.1 |
| 言語 | Python | 3.12.9 |
| Supabase クライアント | supabase-py | 2.16.0 |
| バリデーション | Pydantic v2 | 2.10.3 |
| メール送信 | Resend SDK | 2.10.0 |
| 環境変数 | python-dotenv | 1.0.1 |

### インフラ・外部サービス

| サービス | 用途 |
|---|---|
| Supabase | PostgreSQL DB + Row Level Security + Supabase Auth |
| Resend | お問い合わせメール通知 |
| Square Online | オンラインストア（外部リンク） |
| Instagram | 各ブランドSNS（外部リンク） |
| 画像ホスティング（任意） | イベント・コラボ画像のURLを直接参照 |

---

## 3. ネットワーク構成

```
[ユーザーブラウザ]
        │ HTTPS
        ▼
[本番サーバー]
  ┌─────────────────────────────────────┐
  │ FastAPI (uvicorn)                   │
  │  ├── /api/*    → API処理           │
  │  └── /assets/* → Viteビルド成果物  │
  └─────────────────────────────────────┘
        │ HTTPS (Supabase SDK)
        ▼
[Supabase Cloud (PostgreSQL)]
        
        │ HTTPS (Resend SDK)
        ▼
[Resend API]
```

---

## 4. 環境変数

### `frontend/.env`

| 変数名 | 公開 | 説明 |
|---|---|---|
| `VITE_SUPABASE_URL` | フロントエンドに公開される | Supabase プロジェクトURL |
| `VITE_SUPABASE_ANON_KEY` | フロントエンドに公開される（安全） | Supabase 匿名キー（RLSで保護） |

### `backend/.env`

| 変数名 | 公開 | 説明 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | 非公開 | Supabase プロジェクトURL |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 非公開 | 匿名キー（公開読み取り用） |
| `SUPABASE_SERVICE_ROLE_KEY` | **絶対に非公開** | サービスロールキー（RLSバイパス・管理用） |
| `RESEND_API_KEY` | **絶対に非公開** | Resend APIキー |
| `CONTACT_TO_EMAIL` | 非公開 | お問い合わせ受信メールアドレス |
| `CONTACT_FROM_EMAIL` | 非公開 | お問い合わせ送信元メールアドレス |

> **重要**: `.env` ファイルは `.gitignore` で管理。リポジトリに絶対にコミットしないこと。

---

## 5. デプロイ手順（概要）

```
1. フロントエンドビルド
   cd frontend && npm run build
   → frontend/dist/ に成果物生成

2. 環境変数設定
   backend/.env に本番の接続情報を設定

3. サーバー起動
   cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000

4. DB マイグレーション（初回のみ）
   Supabase SQL エディタで backend/migrations/002_admin_features.sql を実行
```

---

## 6. 管理者認証フロー

```
/admin アクセス
    │
    ├── AdminLayout: supabase.auth.getSession() 実行
    │       │
    │       ├── session あり → Admin UI 表示
    │       └── session なし → ログインフォーム表示
    │                               │
    │               メール + パスワード入力
    │                               │
    │           supabase.auth.signInWithPassword()
    │                               │
    │               ├── 成功 → セッション確立 → Admin UI
    │               └── 失敗 → エラーメッセージ表示

バックエンドAPI（Admin操作）:
    SUPABASE_SERVICE_ROLE_KEY で admin_supabase を生成 → RLS バイパス
    ※ バックエンドAPIへのリクエスト認証は現フェーズ未実装
```

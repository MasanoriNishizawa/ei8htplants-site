# BD-03 — 論理データモデル (Logical Data Model)

## 1. 概念ER図

主要エンティティとその関係性（概念レベル）。

```
┌──────────────┐       ┌────────────────┐
│    Event     │ 1   N │  EventImage    │
│  イベント情報 │──────►│  イベント画像  │
└──────────────┘       └────────────────┘
       │
       │ 1   N
       ▼
┌────────────────────┐
│ WorkshopReservation│
│  ワークショップ予約 │
└────────────────────┘


┌──────────────┐
│ GalleryImage │   独立エンティティ
│  ギャラリー  │   （ブランド属性でフィルタリング）
└──────────────┘


┌──────────────┐
│  Stockist    │   独立エンティティ
│  取扱店      │   （ブランド配列属性を保持）
└──────────────┘


┌──────────────────┐
│  Collaboration   │   独立エンティティ
│  コラボレーション │
└──────────────────┘


┌──────────────┐
│   Contact    │   独立エンティティ
│ お問い合わせ │   （既読管理フラグ付き）
└──────────────┘


┌──────────────┐       ┌────────────────┐
│   Product    │ 1   N │   OrderItem    │
│    商品      │──────►│   注文明細     │
└──────────────┘       └───────┬────────┘
                               │ N   1
                               ▼
                        ┌──────────────┐
                        │    Order     │
                        │    注文      │
                        └──────────────┘


┌──────────────┐       ┌────────────────────┐
│   Article    │ N   N │    Product         │
│ ジャーナル記事│──────►│（article_product_links 中間テーブル）│
└──────────────┘       └────────────────────┘
```

---

## 2. エンティティ定義

### Event（イベント情報）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| イベント名 | 必須 | テキスト |
| 開始日 | 必須 | 日付 |
| 終了日 | 複数日の場合 | 日付（任意） |
| 開催時間 | 例: 11:00〜17:00 | テキスト（任意） |
| 開催場所 | 必須 | テキスト |
| ブース番号 | | テキスト（任意） |
| 住所 | | テキスト（任意） |
| 公式URL | | URL（任意） |
| 関連ブランド | 複数選択可 | テキスト配列 |
| WS有無フラグ | | 真偽値 |
| WS予約制フラグ | | 真偽値 |
| 過去イベントフラグ | | 真偽値 |
| 表示順 | | 整数 |
| 作成日時 | | タイムスタンプ |

### EventImage（イベント画像）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| イベントID | Event への参照 | UUID（FK） |
| 画像URL | | テキスト |
| 表示順 | | 整数 |

### GalleryImage（ギャラリー画像）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| 画像URL | | テキスト |
| ALTテキスト | | テキスト（任意） |
| ブランド | フィルタリング用 | テキスト（任意） |
| 表示順 | | 整数 |

### Stockist（取扱店）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| 店舗名 | 必須 | テキスト |
| エリア | 例: 東京 | テキスト（任意） |
| 住所 | | テキスト（任意） |
| 店舗URL | | URL（任意） |
| 取扱ブランド | 複数選択可 | テキスト配列 |
| 表示順 | | 整数 |

### WorkshopReservation（予約）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| イベントID | Event への参照 | UUID（FK） |
| 予約者名 | 必須 | テキスト |
| メールアドレス | 必須 | テキスト |
| 電話番号 | | テキスト（任意） |
| 参加人数 | デフォルト1 | 整数 |
| 備考 | | テキスト（任意） |
| ステータス | pending/confirmed/cancelled | テキスト |
| 受付日時 | | タイムスタンプ |

### Collaboration（コラボレーション）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| タイトル | 必須 | テキスト |
| コラボ相手 | | テキスト（任意） |
| 説明文 | | テキスト（任意） |
| 動画URL | | URL（任意） |
| 画像URL | | URL（任意） |
| 開催日 | | 日付（任意） |
| 表示順 | | 整数 |

### Contact（お問い合わせ）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| 名前 | 必須 | テキスト |
| メールアドレス | 必須 | テキスト |
| メッセージ本文 | 必須 | テキスト |
| 既読フラグ | デフォルト: 未読 | 真偽値 |
| 受信日時 | | タイムスタンプ |

### Product（商品）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| 商品名 | 必須 | テキスト |
| 説明文 | BlockEditor JSON | テキスト（任意） |
| 価格 | 税込・円 | 整数 |
| 在庫数 | 0以上 | 整数 |
| 画像URL配列 | | テキスト配列 |
| タグ | 検索補助 | テキスト配列 |
| カテゴリー | アガベ/塊根植物/灌木/サボテン/観葉植物/ハビタットスタイル/ハビタットスタイル資材/園芸資材/鉢 | テキスト（任意） |
| 公開フラグ | false時は公開側に表示しない | 真偽値 |
| 表示順 | | 整数 |
| 作成日時 | | タイムスタンプ |

### Order（注文）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| 顧客名 | 必須 | テキスト |
| メールアドレス | 必須 | テキスト |
| 電話番号 | | テキスト（任意） |
| 郵便番号 | 必須 | テキスト |
| 都道府県 | 必須 | テキスト |
| 市区町村 | 必須 | テキスト |
| 住所1 | 必須 | テキスト |
| 住所2 | マンション名等 | テキスト（任意） |
| 備考 | | テキスト（任意） |
| 小計 | | 整数 |
| 送料 | | 整数 |
| 合計 | | 整数 |
| ステータス | paid/shipped/cancelled | テキスト |
| Square決済ID | | テキスト（任意） |
| 作成日時 | | タイムスタンプ |

### OrderItem（注文明細）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| 注文ID | Order への参照 | UUID（FK） |
| 商品ID | Product への参照（削除時NULL） | UUID（FK, nullable） |
| 商品名スナップショット | 注文時の名前を保持 | テキスト |
| 価格スナップショット | 注文時の価格を保持 | 整数 |
| 数量 | 1以上 | 整数 |
| 作成日時 | | タイムスタンプ |

### Article（ジャーナル記事）

| 属性 | 説明 | 型 |
|---|---|---|
| ID | 一意識別子 | UUID |
| タイトル | 必須 | テキスト |
| 本文 | BlockEditor JSON | テキスト（任意） |
| 画像URL配列 | OGP・サムネイル用 | テキスト配列 |
| タグ | フィルタリング用 | テキスト配列 |
| 公開日時 | 設定された場合のみ表示 | タイムスタンプ（任意） |
| 作成日時 | | タイムスタンプ |

---

## 3. エンティティ間の関係

| 関係 | 多重度 | 説明 |
|---|---|---|
| Event — EventImage | 1対多 | 1つのイベントに複数の画像が紐付く |
| Event — WorkshopReservation | 1対多 | 1つのイベントに複数の予約が紐付く |
| Order — OrderItem | 1対多 | 1つの注文に複数の注文明細が紐付く |
| OrderItem — Product | 多対1 | 複数の注文明細が1つの商品を参照（削除時NULL） |
| Article — Product | 多対多 | article_product_links 中間テーブル経由 |
| GalleryImage | 独立 | イベントとは独立。ブランド属性でフィルタリング |
| Stockist | 独立 | ブランドを属性（配列）として保持 |
| Collaboration | 独立 | イベントとは独立したコンテンツ |
| Contact | 独立 | イベントとは無関係の問い合わせ |
| Product | 独立 | カテゴリー・タグで分類 |

---

## 4. ブランド属性の設計方針

ブランドは `TEXT[]` 配列として各テーブルに直接格納する（正規化なし）。  
理由: ブランド数は固定3種で変更の可能性が低く、正規化のオーバーヘッドを避けるため。

| ブランド値 | 意味 |
|---|---|
| `'ei8ht plants'` | ei8ht plants ブランド |
| `'Habitat Oides'` | Habitat Oides ブランド |
| `'HUE by ei8ht plants'` | HUE by ei8ht plants ブランド |

物理テーブル定義は `doc/DD/05_データベース詳細.md` を参照。

# DD-01 — モジュール構成

## 1. フロントエンド コンポーネントツリー

```
index.tsx
└── <RouterProvider router={router} />
    └── App.tsx  (createBrowserRouter)
        ├── /  →  Layout.tsx
        │   ├── Header.tsx
        │   ├── <Outlet />
        │   │   ├── /                  Home.tsx
        │   │   ├── /events            Events.tsx
        │   │   ├── /gallery           Gallery.tsx
        │   │   ├── /concept           Concept.tsx
        │   │   ├── /contact           Contact.tsx
        │   │   ├── /stockists         Stockists.tsx
        │   │   ├── /collaborations    Collaborations.tsx
        │   │   ├── /reserve           Reserve.tsx
        │   │   ├── /ei8htplants       brands/Ei8htPlants.tsx
        │   │   ├── /habitatoides      brands/HabitatOides.tsx
        │   │   ├── /habitatoides/workshop  brands/HabitatOidesWorkshop.tsx
        │   │   └── /hue               brands/Hue.tsx
        │   └── Footer.tsx
        └── /admin  →  AdminLayout.tsx
            ├── <Outlet />  (認証済み時のみ)
            │   ├── /admin             Dashboard.tsx
            │   ├── /admin/events      admin/Events.tsx
            │   │   └── /admin/events/new   admin/EventForm.tsx
            │   │   └── /admin/events/:id   admin/EventForm.tsx
            │   ├── /admin/gallery     admin/Gallery.tsx
            │   ├── /admin/stockists   admin/Stockists.tsx
            │   ├── /admin/reservations  admin/Reservations.tsx
            │   ├── /admin/collaborations  admin/Collaborations.tsx
            │   └── /admin/contacts    admin/Contacts.tsx
            └── <LoginForm />  (未認証時のみ)
```

**共有コンポーネント**

| コンポーネント | 使用先 |
|---|---|
| `EventCard.tsx` | `Home.tsx`, `Events.tsx` |
| `Layout.tsx` | 全公開ページの親 |
| `Header.tsx` | `Layout.tsx` 内 |
| `Footer.tsx` | `Layout.tsx` 内 |

---

## 2. フロントエンド モジュール依存グラフ

```
pages/* ──────────────────────────────────────────┐
                                                   ▼
brands/* ──────────────────────────────────────► lib/api.ts
                                                   │
admin/* ────────────────────────────────────────── ┘
                                                   │
components/EventCard.tsx ──────────────────► fetch(BASE + path)
                                                   │
                                                 /api/*
                                                   │
                                           Vite proxy (dev)
                                         FastAPI (port 8000)
```

- `lib/api.ts` が唯一のAPIアクセス窓口。ページは `api.xxx.yyy()` のみを呼ぶ。
- `@supabase/supabase-js` は `admin/AdminLayout.tsx` のみが直接使用（Auth用）。
- CSS は `index.css`（グローバル）+ インラインスタイルの2層構成。Tailwindクラスは一部ページで使用。

---

## 3. バックエンド モジュール依存グラフ

```
main.py
├── CORSMiddleware (localhost:5173 許可)
├── StaticFiles (本番: frontend/dist/assets, img, favicon)
├── include_router(events.router,  prefix='/api')
├── include_router(gallery.router, prefix='/api')
├── include_router(stockists.router, prefix='/api')
├── include_router(contact.router,  prefix='/api')
├── include_router(reserve.router,  prefix='/api')
└── include_router(collaborations.router, prefix='/api')

routes/events.py
routes/gallery.py      ──── db.py ──── config.py ──── .env
routes/stockists.py          │
routes/contact.py ──── resend SDK
routes/reserve.py
routes/collaborations.py

db.py
├── supabase  = create_client(URL, ANON_KEY)    # 公開読み取り
└── admin_supabase = create_client(URL, SERVICE_ROLE_KEY)  # 管理書き込み
```

- `supabase`（anon）: GET系エンドポイントが使用。RLSのpublic readポリシーに従う。
- `admin_supabase`（service_role）: POST/PUT/PATCH/DELETE が使用。RLSをバイパス。
- 各routeファイルはPydantic `BaseModel` でリクエストバリデーションを行う。

---

## 4. リクエスト データフロー

```
ブラウザ
  │  fetch('/api/...')
  ▼
Vite devserver (port 5173)  ※開発時のみ
  │  proxyRequest → localhost:8000
  ▼
FastAPI (uvicorn, port 8000)
  │  router dispatch
  ▼
routes/*.py
  │  Pydantic validation
  │  supabase / admin_supabase
  ▼
Supabase (PostgreSQL + RLS)
  │
  ▼  レスポンスJSON
routes/*.py
  │
FastAPI
  │  JSONResponse
  ▼
ブラウザ  lib/api.ts → res.json() → TypeScript型
```

**本番時**: Vite proxyは不要。FastAPI が `frontend/dist` を StaticFiles で配信し、`/{full_path:path}` でSPAのindex.htmlにフォールバック。

---

## 5. ファイルツリー（実装ファイルのみ）

```
ei8htplants-site/
├── frontend/
│   ├── src/
│   │   ├── App.tsx
│   │   ├── index.css
│   │   ├── main.tsx  (エントリーポイント)
│   │   ├── components/
│   │   │   ├── EventCard.tsx
│   │   │   ├── Header.tsx
│   │   │   ├── Layout.tsx
│   │   │   └── Footer.tsx
│   │   ├── lib/
│   │   │   └── api.ts        # APIクライアント + 全型定義
│   │   └── pages/
│   │       ├── Home.tsx
│   │       ├── Events.tsx
│   │       ├── Gallery.tsx
│   │       ├── Concept.tsx
│   │       ├── Contact.tsx
│   │       ├── Stockists.tsx
│   │       ├── Collaborations.tsx
│   │       ├── Reserve.tsx
│   │       ├── brands/
│   │       │   ├── Ei8htPlants.tsx
│   │       │   ├── HabitatOides.tsx
│   │       │   ├── HabitatOidesWorkshop.tsx
│   │       │   └── Hue.tsx
│   │       └── admin/
│   │           ├── AdminLayout.tsx
│   │           ├── Dashboard.tsx
│   │           ├── Events.tsx
│   │           ├── EventForm.tsx
│   │           ├── Gallery.tsx
│   │           ├── Stockists.tsx
│   │           ├── Reservations.tsx
│   │           ├── Collaborations.tsx
│   │           └── Contacts.tsx
│   ├── public/img/             # ロゴ等の静的画像
│   ├── vite.config.ts
│   ├── tsconfig.json
│   ├── package.json
│   └── .env                    # VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
│
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── config.py
│   │   ├── db.py
│   │   └── routes/
│   │       ├── __init__.py
│   │       ├── events.py
│   │       ├── gallery.py
│   │       ├── stockists.py
│   │       ├── contact.py
│   │       ├── reserve.py
│   │       └── collaborations.py
│   ├── migrations/
│   │   └── 002_admin_features.sql
│   ├── requirements.txt
│   └── .env                    # SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY/RESEND_API_KEY等
│
└── doc/
    ├── RD/   # 要件定義
    ├── BD/   # 基本設計
    └── DD/   # 詳細設計（本書）
```

# PM-04 — 課題・リスク管理表 (Issue & Risk Log)

## 1. 現在の課題（Issue）

| ID | 課題 | 優先度 | 状態 | 対応方針 |
|---|---|---|---|---|
| I-03 | display_order の同時書き込み競合 | 低 | 許容中 | 単一管理者運用のため現状許容。多管理者対応時はトランザクションで解決 |

---

## 2. リスク管理（Risk）

| ID | リスク | 発生確率 | 影響度 | 対策 |
|---|---|---|---|---|
| R-01 | Supabase 障害によるサイト全停止 | 低 | 高 | Supabase は99.9% SLA。重大障害時は一時的にメンテナンスページへ切り替え |
| R-02 | SERVICE_ROLE_KEY の漏洩 | 低 | 致命的 | `.gitignore` 管理を徹底。漏洩時は即座にSupabase管理画面でキーをローテーション |
| R-03 | Resend API 上限超過によるメール不達 | 低 | 中 | 月次の問い合わせ件数を確認し、上限接近時はプランアップグレード |
| R-04 | 外部ホスティング画像のリンク切れ | 低 | 低 | 画像URLは外部依存。Admin Gallery の「URL無効」バッジで定期確認し、切れた画像は再登録する |
| R-05 | 管理者パスワード忘れ | 低 | 高 | Supabase管理画面からパスワードリセット可能。手順を `doc/OP/02_運用・保守マニュアル.md` に記載 |
| R-06 | 単一開発者による引き継ぎリスク | 中 | 高 | 設計書（RD/BD/DD）の整備により、第三者への引き継ぎを可能にする |

---

## 3. 解決済み課題

| ID | 課題 | 解決日 | 解決内容 |
|---|---|---|---|
| I-01 | バックエンドAPIに認証がない | 2026-07 | `backend/app/auth.py` に `require_auth` Depends を作成し、全Admin操作エンドポイントに適用。フロントは `authRequest` で Supabase JWT を Bearer トークンとして送信 |
| I-02 | 予約確認メールの自動送信なし | 2026-07 | `reserve.py` の `create_reservation` で予約保存後に Resend でお客様へ確認メールを送信 |
| I-04 | お問い合わせ更新のエラーフィードバックなし | 2026-07 | `Contacts.tsx` の `toggleRead` を try/catch で囲み、失敗時にエラーメッセージを3秒表示 |
| I-05 | 画像URLの有効性チェックなし | 2026-07 | `Gallery.tsx` の `<img>` に `onError` を追加し、URLが無効な画像に「URL無効」バッジを表示 |
| I-06 | `contacts` テーブルへの anon INSERT は不要 | 2026-07 | `backend/migrations/003_fix_rls.sql` で `"anon insert contacts"` ポリシーを削除 |
| I-X01 | Admin予約管理ページが `/api/reservations` を呼んでいた（正: `/api/reserves`） | 2026-07 | `api.reserve.list()` を使用するよう修正 |
| I-X02 | 取扱店の編集機能が未実装 | 2026-07 | PATCH API + 編集モーダルを実装 |
| I-X03 | ギャラリーのブランドフィルタリング未実装 | 2026-07 | gallery.py にクエリフィルタを追加、フロントにタブUIを実装 |
